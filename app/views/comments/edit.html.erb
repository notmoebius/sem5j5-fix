<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Editer un commentaire </h1>
    <p class="lead">formulaire de modification ci-dessous</p>
  </div>
</div>

<%= form_tag url_for(action: 'update'), method: "put" do %>

  <%= label_tag 'Contenu du commentaire : ' %>
  <%= text_area_tag 'content', @comment.content %>
  <br /> <br />

  <%= submit_tag "Mettre à jour", class: "btn btn-secondary " %>

<% end %>